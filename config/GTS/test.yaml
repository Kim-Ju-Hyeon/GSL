---
exp_name: 1
exp_dir: ./exp
use_gpu: False
device: cpu
seed: 1010
model_name: GTS

dataset:
  root: ./data/spike_lambda
  name: spike_lambda
  time_steps: 500
  pred_steps: 20
  window: 100
  train_step: 4000
  validation_step: 4400
  test_step: 4800

train:
  optimizer: Adam
  epoch: 1
  loss_function : MSELoss
  lr: 1.0e-3
  momentum: 0.9
  wd: 0.0e-4
  batch_size : 128
  lr_decay: 0.1
  lr_decay_steps: [10000]


node_features: 1
output_dim: 1
hidden_dim: 16
nodes_num: 100

kernel_size: 10
stride: 1
conv1_dim: 8
conv2_dim: 16
fc_dim: 15712

diffusion_k: 3
num_layer: 1

batch_size: 128
encoder_length: 70
decoder_length: 30

tau: 0.3
use_teacher_forcing: False
teacher_forcing_ratio: 0.5


